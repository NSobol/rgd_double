// initial state ----------------------------------------------------
import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const order = createSlice({
  name: 'order',
  initialState,
  reducers: {},
});

export default order.reducer;
